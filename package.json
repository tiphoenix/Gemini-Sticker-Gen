{
  "name": "sticker-gen",
  "version": "1.0.0",
  "description": "Sticker Generator Full Stack",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
